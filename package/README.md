# package/

The artifact under audit. `MillenniumLean/` is the verbatim extraction of
`MillenniumLean_ClayProof_20260901.zip` (AIX Global, Zenodo
10.5281/zenodo.22226553). SHA-256 of every file: `../data/audit_report.json`.

All quotations in the audit anchor to these exact bytes. The `.lake/`
subtree is local build state (dependency checkouts); it is not evidence.
