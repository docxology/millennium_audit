# package/ - Agent Notes

EVIDENCE - DO NOT MODIFY. Nothing under `MillenniumLean/` (source, configs,
docs) may be edited, reformatted, or "fixed": the audit's quotations are
line-anchored to these bytes, and any change invalidates the verification
receipt. Build-state directories (`.lake/`) may be regenerated for kernel
reproduction but never for content changes. The original zip sits at the
project root and remains the transferable provenance artifact.

# package/MillenniumLean - Verbatim Evidence (no doc pairs inside)

This tree is the extracted Zenodo artifact under audit and is intentionally
left byte-exact; no AGENTS.md/README.md pair is added INSIDE it (that would
modify the evidence). Its role and handling rules live in ../AGENTS.md and
../README.md.
