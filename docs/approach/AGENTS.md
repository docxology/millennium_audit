# approach/ - Agent Notes

This directory documents METHOD, not findings. Keep it synchronized with
what was actually done: if the audit process changes (new verification
layer, changed classification), update audit-doctrine.md in the same change
that implements it. The doctrine's verification chain is a checklist - each
layer named there should have a real artifact or command behind it.
