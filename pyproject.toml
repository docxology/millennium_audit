[project]
name = "millennium-audit"
version = "1.0.0"
description = "Forensic audit of the MillenniumLean Clay-proof package (AIX Global)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
